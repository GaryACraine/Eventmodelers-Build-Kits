services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: dcb
      POSTGRES_PASSWORD: dcb
      POSTGRES_DB: dcb
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U dcb"]
      interval: 5s
      timeout: 5s
      retries: 5
