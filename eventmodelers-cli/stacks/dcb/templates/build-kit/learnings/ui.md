# UI learnings

What later UI jobs in this project should know, that `build-screen` doesn't already say: its pages, components,
mockup conventions and tests. One lesson per bullet with the project's example; correct or delete a wrong one;
at most 40 (ADR-028).
